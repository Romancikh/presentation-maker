import MenuBar from "../MenuBar/MenuBar.tsx";
import ToolBar from "../ToolBar/ToolBar.tsx";

function Header() {
  return (
    <div>
      <MenuBar />
      <ToolBar />
    </div>
  );
}

export default Header;
