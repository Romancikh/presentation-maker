import "./App.css";
import { Presentation } from "./types/types.ts";

type AppProps = {
  presentation: Presentation;
};

function App({ presentation }: AppProps) {
  return <></>;
}

export default App;
